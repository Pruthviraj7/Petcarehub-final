@{
    ViewBag.Title = "Create Veteran";
}

<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .header {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            margin-top: 15px;
        }

            input[type="submit"]:hover {
                background-color: #0056b3;
            }
    </style>
</head>
<body>
    <div class="container">
        <h2 class="header">Create Veteran</h2>
        <form id="veteranForm" action="/Veteran/Create" method="POST" onsubmit="return validateForm()">
            <div>
                <label>Name:</label> <input type="text" name="VeteranName" required />
            </div>
            <div>
                <label>Specialization:</label> <input type="text" name="VeteranSpecialization" required />
            </div>
            <div>
                <label>Phone:</label> <input type="text" name="VeteranPhone" required />
            </div>
            <div>
                <label>Email:</label> <input type="text" name="VeteranEmail" required />
            </div>
            <div>
                <label>Address:</label> <input type="text" name="VeteranAddress" required />
            </div>
            <div>
                <label>Rating:</label> <input type="number" name="VeteranRating" step="0.01" required />
            </div>
            <div>
                <input type="submit" value="Add Veteran" />
            </div>
        </form>
    </div>

    <script>
        function validateForm() {
            var form = document.getElementById('veteranForm');
            if (!form.checkValidity()) {
                // If the form is invalid, display appropriate error messages
                form.reportValidity();
                return false;
            }
            return true;
        }
    </script>
</body>
</html>
